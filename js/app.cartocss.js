var app = (function(parent){ 
  // cartocss for styling the data layer from CartoDB

  parent.el.cartocss = {
     "taxLots" : "#nyc_flips_pluto_150712{polygon-fill:#A53ED5;polygon-opacity:1;line-color:#FFF;line-width:0;line-opacity:0;}"
   };

  return parent;

})(app || {});
